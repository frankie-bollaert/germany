# Downloaded data — rebuilt from the sources by the download_* scripts, never committed.
# DRY_RUN writes here too: each downloader spools its index and aria2 input list before it
# decides whether to transfer anything.
*_lidar/
samples/
alkis/
lidar/
by_parzellarkarte/
.download_all.failures

# Derived from that data by alkis_to_duckdb.sh — rebuildable, so never committed either.
# (Its GeoParquet staging lives under alkis/.duckdb-stage/, already covered above.)
*.duckdb
*.duckdb.wal
*.duckdb.tmp/
export/

# macOS / IDE / agent-local state
.DS_Store
.idea/
.claude/worktrees/
.claude/settings.local.json
